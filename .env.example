# Parser dashboard — server-only (never expose to the client bundle)
PARSER_LOGIN=
PARSER_PASSWORD=
PARSER_SESSION_SECRET=

# ЦехУчёт login — server-only
UCHET_LOGIN=
UCHET_PASSWORD=
UCHET_SESSION_SECRET=

# Set to 0 to disable verbose parser debug logs in production
# PARSER_DEBUG=1
